spring:
  application:
    name: kodif-home-work

server:
  port: 80

springdoc:
  api-docs:
    path: "/api-docs"
  swagger-ui:
    path: "/swagger"

#  endpoints:
#    web:
#      exposure:
#        include: health, info, metrics, env, beans
